// e01 · 语法错误集 —— 故意写错，验证解析器报错的行列号与恢复能力
// 预期：非零退出码 + 多条 Parse error（每处错误的行列应精确指向下方标注处）。

// [1] 缺分号
number a = 1

// [2] 括号不匹配
number b = (2 + 3;

// [3] 花括号不闭合的 if
if (a > 0) {
    print("ok");

// [4] 多余的右花括号
}

// [5] 关键字当变量名（word 是保留字）
number word = 5;

print("如果看到这行，说明语法错误未被拦截 —— 这是 BUG");
