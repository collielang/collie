// e03 · 运行时错误集（主文件）—— 数组越界
// 预期：前两行正常输出后，非零退出 + 带行列号的运行时诊断；第三行不输出。

array arr = [10, 20, 30];
print("越界前：正常执行");
print(arr[2]);        // 30，合法
print(arr[3]);        // 越界 —— 此处中止
print("如果看到这行，说明越界未被拦截 —— 这是 BUG");
